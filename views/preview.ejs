<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Preview Registration</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-4">
        <h2 class="text-center">Preview Your Information</h2>
        <form action="/submit" method="POST">
            <div class="p-4 border rounded bg-light shadow">
                <p><strong>Roll Number:</strong> <%= formData.roll_number %></p>
                <p><strong>Membership Type:</strong> <%= formData.membership_type %></p>
                <p><strong>Family Name:</strong> <%= formData.family_name %></p>
                <p><strong>First Name:</strong> <%= formData.first_name %></p>
                <p><strong>Middle Name:</strong> <%= formData.middle_name || 'N/A' %></p>
                <p><strong>Gender:</strong> <%= formData.gender %></p>
                <p><strong>Mobile Phone:</strong> <%= formData.mobile_phone %></p>
                <p><strong>Email Address:</strong> <%= formData.email_address %></p>
                <p><strong>Birthday:</strong> <%= formData.birthday %></p>
                <p><strong>Address:</strong> <%= formData.address %></p>
                <p><strong>Municipality:</strong> <%= formData.municipality %></p>
                <p><strong>Barangay:</strong> <%= formData.baranggay || 'N/A' %></p>
                <p><strong>Province:</strong> <%= formData.province %></p>
                <p><strong>Zip Code:</strong> <%= formData.zip || 'N/A' %></p>
                <p><strong>ID Type:</strong> <%= formData.id_type %></p>

                <h3>Uploaded Images</h3>
                <div class="row">
                    <div class="col-md-4 text-center">
                        <p><strong>Selfie:</strong></p>
                        <% if (formData.files.selfie) { %>
                            <img src="/<%= formData.files.selfie %>" alt="Selfie" class="img-thumbnail" width="150">
                        <% } %>
                    </div>
                    <div class="col-md-4 text-center">
                        <p><strong>ID Front:</strong></p>
                        <% if (formData.files.id_front) { %>
                            <img src="/<%= formData.files.id_front %>" alt="ID Front" class="img-thumbnail" width="150">
                        <% } %>
                    </div>
                    <div class="col-md-4 text-center">
                        <p><strong>ID Back:</strong></p>
                        <% if (formData.files.id_back) { %>
                            <img src="/<%= formData.files.id_back %>" alt="ID Back" class="img-thumbnail" width="150">
                        <% } %>
                    </div>
                </div>

                <p><strong>Agree to Terms:</strong> <%= formData.agree_to_terms %></p>

                <div class="text-center mt-3">
                    <button type="submit" class="btn btn-success">Confirm & Submit</button>
                    <a href="/register" class="btn btn-warning">Edit Information</a>
                </div>
            </div>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
